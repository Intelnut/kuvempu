{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "/login.json",
    "type": "object",
    "title": "User Login",
    "description": "Schema to authenticate an user with email and password",
    "required": [
        "email_id",
        "password"
    ],
    "properties": {
        "email_id": {
            "$id": "#/properties/email_id",
            "type": "string",
            "title": "Email Id",
            "description": "Email Id of the user",
            "format": "email"
        },
        "password": {
            "$id": "#/properties/password",
            "type": "string",
            "title": "Password",
            "description": "Password associated with the email id"
        }
    }
}